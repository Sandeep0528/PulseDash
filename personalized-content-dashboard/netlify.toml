[build]
  base = "personalized-content-dashboard"
  # We force NODE_OPTIONS to be empty and restrict CPUs to 1 at the system level
  command = "export NODE_OPTIONS='' && export CIRCLECI=true && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"